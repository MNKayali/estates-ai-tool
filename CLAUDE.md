# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

> The line above is intentional and important: this repo runs **Next.js 16.3.5**, which
> has breaking changes vs. older versions. Read the relevant guide in
> `node_modules/next/dist/docs/` before writing framework code. (Bumped from 16.2.6 in
> September 2026 to fix a critical RCE — see "Recent Work & Status" below. Both are
> within the 16.x line; nothing about the Next 16 migration notes changed.)

> **September 2026 re-alignment:** the rates workbook is now **NRM1 v5.x** — three sheets
> ('1. Instructions', '2. Scope and Rates', '3. Settings'); the committed file
> `NRM1_Cost_Estimate_Tool_v5_2.xlsx` holds **v5.4** (the filename is kept on purpose so
> `RATES_FILE_URL` never has to change for a content update). The programme workbook is
> **Programme v4.4** (file still `Estates_AI_Programme_v4_3.xlsx`, for the same reason), and the questionnaire is **v7** with the v5 scope picker. The
> cost engine and the scope picker were rebuilt from "Scope of works — brief for Claude
> Code" (`docs/scope-of-works-brief-2026-09-23.md`). If anything here contradicts what you
> read in the source files, trust the source files.

## Current state — read this first (23 September 2026)

**User accounts replace the access code (25 September 2026, branch `claude/loving-hamilton-xfzkro`).** Invite-only email + password accounts; each user sees their own reports at `/reports`, kept until they delete them. Decisions (user's): email + password, invite only, keep reports until deleted.
- `lib/session.js` — signed `projento_session` cookie (`{uid, ver, exp}` + HMAC with `COOKIE_SECRET`, 30 days, fails closed in production without the secret). `lib/users.js` — accounts in KV (`user:<uid>`, `user:email:<email>`, `users:index`), scrypt hashes, one-time invite (7 d) / reset (1 h) links stored as SHA-256 only. `lib/auth.js` — `getSessionUser()` (checks the account is active and `sessionVersion` matches, so a password change or disable signs out every device), `authoriseReport()` (owner or admin; someone else's report is a 404). `lib/email.js` — Resend by plain fetch, optional.
- `proxy.ts` checks the session against KV (Next 16 proxy runs on Node). The admin cookie is also accepted on report pages/APIs and `/api/rates-check`. `/access` redirects to `/login`; `/api/check-access` is gone.
- Reports carry `ownerId`; `finaliseReport()` stores an owned report **with no TTL** and adds a summary to the hash `user:<uid>:reports`. Reports without an owner (pre-accounts) keep the 90-day TTL and stay viewable by any signed-in user. Only the owner can delete (`DELETE /api/reports/[id]`).
- Admin → Users: invite (name + email), new set-up / reset link, disable / enable. Links are always shown to the admin, and emailed when `RESEND_API_KEY` + `EMAIL_FROM` are set.
- The PDF route hands headless Chromium the caller's own cookies (to the server-controlled origin only).
- Tests: `lib/__tests__/auth.test.js` with an in-memory KV (`helpers/fakeKv.js`). Verified end to end against a local fake Upstash REST server (invite → set password → sign in → generate → finalise → list → PDF → delete; other user gets 404; disable / password change sign out other sessions).
- **To deploy:** set `COOKIE_SECRET` (already required), optionally `RESEND_API_KEY` / `EMAIL_FROM` (verify projento.co.uk in Resend) and `APP_ORIGIN=https://www.projento.co.uk`; `ACCESS_CODE` is no longer read. Then sign in to /admin and invite yourself first.

**Test-report run and fixes (24 September 2026, branch `fix/test-report-issues`).** 21 test scenarios (3 per project type, every building use at least twice) were written by agents, entered through the live questionnaire, and each PDF was reviewed against its answers: 76 issues (21 High). Everything for that run lives in `test-reports/` — `<ID>/scenario.md` (story, answers, "what the report should show"), `plan.json`, `review.md`, `report-ids.json`, and the tools in `_reference/` (`build-reference.mjs` what the form offers per type; `build-plans.mjs` scenario → validated plan; `filler.js` + `snippet.mjs` the in-page form filler; `rerun.mjs` re-runs every scenario through the deterministic pipeline locally — run it with `node --import ./test-reports/_reference/alias.mjs`; `combine-reviews.mjs` the issues table). The PDFs, reviews and `Issues.xlsx` were saved to the user's `Downloads\Agent test`. Fixed on the branch, each with a regression test:
- **Stale budget warning.** `runSenseCheck` runs before the final confidence-linked range pass, so its `BUDGET_SHORTFALL` text described a range the report never printed; the AI and the risk register quoted it ("£2,000 shortfall" beside a budget box saying the budget fits). `refreshBudget()` in `lib/senseCheck.js` now rewrites the warning from the final cost (grades unchanged).
- **Regions.** ▶ location_factors had **no South West row** and no L, CA, MK, TF, HS, ZE — all priced at the West Midlands default. Workbook **v5.4** adds them (South West factors 0.94/1.00/0.97 are an AI estimate to verify against BCIS) and splits SY by district. The old matcher cut the postcode at its first digit, so the Inner London district rows (SW1, SE1, E1W) never matched; `lib/postcodeRegion.js` (most specific entry wins) is now shared by the cost engine and the questionnaire.
- **Financial Case** shows whenever Q5.1 names a benefit (it needed Q5.2 before); payback only with an annual figure; the mid-point is `cost.total.mid`, not the average of the asymmetric range ends (`calcRoi` in `lib/reportContent.js`, also used by the prompt).
- **Planning after a completed Stage 3** is no longer dropped: the application goes in at the end of Stage 3, so the determination runs alongside Stage 4 and holds tender if it outlasts it (`lib/programmeCalculator.js`).
- **Programme workbook v4.4** adds Durations rows SV7 (ecology) and BS1 (Gateway 2), from `docs/workbook-changes-sept-2026.md` (weeks illustrative).
- **Report wording:** specification printed only where Q2.4 was asked (`specShown`); Q2.5 standards on the scope line; the "Other" building-use description on the cover; no "0 m² GIFA" on external works; project-cost rows rounded as the engine rounds them so the table foots; "capped at 10%" instead of "-1% for capped at 10%"; "8 trees" not "8 nr trees"; client wording for an unrecognised postcode.
- **Prelims:** restricted working hours are inferred for retail / hospitality only while the premises trade (not vacant), and labelled as assumed.
- **Risk register:** each Q3.1 known issue is a deterministic seed (`KNOWN_ISSUE_RISK_SEEDS` in `lib/prose.js`); seeds outrank the model's additions under the ten-risk cap; a seed the model tagged with another seed's Ref is re-tagged by its own Ref (they used to collide in `prepareRisks` and the client's known issue vanished).
Deployment: both workbooks are read from `main` by URL, so merging this branch switches production to v5.4 / v4.4 — `/api/rates-check` should then report `NRM1 v5.4 (2026-09-24)`. Not changed (judgement or scenario disagreements, listed in `Downloads\Agent test\Issues.xlsx`): AI phrasing slips (e.g. "gateway sit"), FO-1's typical-scope asbestos line on a 1980s building, programme not scheduling term-time works into holidays, EW-1's missing "no budget stated" line (the summary page has no room without a layout change).

**Report design system (24 September 2026)** — built on `feat/report-design-system` and **merged into PR #12's branch** (`feat/scope-catalogue-v5-2`), so it ships with PR #12. The new A4 report template, approved by the user at both example gates (PDF/screen, then Word opened in Word), with fixed counts and word limits on the AI text. See "Report output" below for how it works. **Staging check done on a Vercel preview (24 September 2026):** Preview `RATES_FILE_URL` points at the branch's v5.3 workbook; a real report generated end to end through KV; screen, Word (9 pages) and PDF (9 pages, Plex embedded) all good. The PDF needed two fixes found only on Vercel: a preview now prints its own pages (`getOrigin()` used the production URL), and `next.config.ts` traces `@sparticuz/chromium/bin` into the PDF route (it was never bundled — the old silent print fallback hid this in production). A failed export now returns `detail` with the reason. Not yet done: the user's review of figures and wording, suggest-scope / compare / phone view on this code, one PDF glyph falling back to Open Sans, and the real contact details / product name for `BRAND` in `lib/reportStyle.js` (placeholders today).

**PR #12** (`feat/scope-catalogue-v5-2` → `main`, https://github.com/MNKayali/estates-ai-tool/pull/12) was **merged on 24 September 2026** after the staging check below, together with the Production `RATES_FILE_URL` switch to the `main` v5_2 workbook. It carries six older commits from `docs/scope-catalogue-spec` that were never pushed on their own (the Q2.2/Q2.3 label swap, the "skipped benchmark can't read as passed" sense-check fix, the 22 Sep catalogue spec, the NRM1 and question maps), then the v5 work: the loader and evaluator; the scope engine, cost switch and picker; both report renderers; docs and sample; the Extension stale-level fix; workbook v5.3; this handoff.

**Deploying it is one release, not two.** The new loader rejects v4.5 and the old code cannot read v5.x:
1. Merge PR #12.
2. In Vercel set `RATES_FILE_URL` (Production) to `https://raw.githubusercontent.com/MNKayali/estates-ai-tool/main/NRM1_Cost_Estimate_Tool_v5_2.xlsx`, redeploy, and change `.env.local` the same way.
3. `/api/rates-check` must report `NRM1 v5.3 (2026-09-23)` (v5.4 once `fix/test-report-issues` is merged) with no errors. Until step 2, production cannot price anything.

**Recommended before merging — a staging check.** Set `RATES_FILE_URL` for **Preview only** to `https://raw.githubusercontent.com/MNKayali/estates-ai-tool/feat/scope-catalogue-v5-2/NRM1_Cost_Estimate_Tool_v5_2.xlsx`, redeploy the PR preview, then on it: generate a report (this is the only way to exercise the KV-backed two-phase path on this code), download the PDF, try "Suggest scope from my objective" and the scenario-comparison panel, and look at the picker on a desktop and a phone.

**Verified in the build session:** 206 Vitest cases (1 skip, a Programme-workbook row not yet added), clean `npm run build`, lint no worse than `main` (every remaining error pre-dates this work); in the browser, locally with no KV: scenario 1 end to end to a generated report (the `.docx` built, the prose quoted the client's 12 + 1 toilets), scenario 5 (External works only), a v4 draft migrating, and an Extension draft losing its hidden level. **Not yet verified:** the KV path, PDF export, a live suggest-scope call and the compare panel on this code; **what the picker looks like** — browser screenshots timed out all session, so nobody has seen `ScopePicker.jsx` rendered; a keyboard / screen-reader pass over it.

**Decisions taken while building — don't re-open them without the user:**
- Other or mixed keeps the refurbishment-then-new-build rate fallback; the workbook's "Per component" split is not used (the area split was dropped earlier).
- Extension no longer asks level of intervention (brief decision 2); a stale answer is cleared in the form and ignored by both engines.
- 'Shown on' decides visibility even when an item has no rate for the type (then it is listed as unpriced).
- A RATE effect bumps a default option once however many sources call for it.
- External works only: Q1.5 is the external works area and GIFA is 0.
- Other or mixed has no typical scope — the catch-all has no honest default (the app never offered one).
- Workbook RISK effects become risk seeds rated Medium / Medium / Medium.
- Pricing on counts estimated from ▶ quantity_ratios lowers confidence by one deficiency until the user enters the real figures.
- v4 drafts and KV reports are migrated through 'Replaces old codes', never discarded.

**Open items, in the order they're worth doing:**
1. The staging check above, then merge + the env-var switch.
2. Verify the 47 rates and every quantity ratio marked "AI estimate – verify" against real project examples (brief decision 8). That is pricing work, not code. The report flags those lines with a † until then.
3. The ▶ range_widths factors (A ±10% to D −25%/+30%) are the illustrative ones from the September spec. They widen most reports' ranges compared with the old ±11%.
4. Not in v5.x yet, from `docs/workbook-changes-sept-2026.md`: the higher-risk-building percentage rows, the extra benchmark bands, and a rate base date (the last needs a small loader change). That doc's Programme half (Procurement sheet, SV7/BS1 rows) is still to apply.
5. The v4.5-era analysis scripts (`scripts/build-question-map.mjs`, `build-question-scope-map.mjs`, `build-scope-relevance-sheet.mjs`) import the removed `getScopeItems()` and no longer run. The `docs/*.xlsx` maps they produced describe v4.5 and are historical.
6. The `scripts/__baseline__` snapshots predate v5 — every number moved in the switch, so re-snapshot before relying on a `scripts/baseline.mjs` diff.
7. The older items under "What's outstanding" at the end of this file.

**Editing the workbook.** The default is still a cell-by-cell spec that the user applies in Excel. When the user asks you to make the edit yourself, use `scripts/xlsx-cells.mjs` (`show` / `set` with the expected old value / `diff`). It edits the sheet XML inside the zip, so styles, drop-downs and formulas survive. **Never round-trip the file through SheetJS.** Then:
- keep the filename;
- bump `Version` on ▶ workbook_info and add an update-log line on '1. Instructions';
- run `diff` against the previous file and `npm test` before committing.

For added rows, write a one-off script that clones an existing row's styles and fixes the sheet's `<dimension>`; v5.3's range_widths rows and update-log line were done that way.

**Files:**
- `docs/scope-of-works-brief-2026-09-23.md` is the brief being built to.
- `NRM1_Cost_Estimate_Tool_v5_2.xlsx` (committed, content v5.4) is the workbook.
- The untracked `NRM1 Cost Estimate Tool v5.2.xlsx` in the repo root is the user's original v5.2 upload. It is stale and not used by anything.

## Commands

```bash
npm run dev        # local dev server at http://localhost:3000
npm run build      # production build (run this to typecheck/validate before pushing)
npm run start      # serve the production build
npm run lint       # eslint (flat config: eslint.config.mjs)
npm test           # vitest — see lib/__tests__/
npm run test:watch # vitest in watch mode
node scripts/dev-open.mjs                 # next dev with sign-in (AUTH_OPEN=1) and the admin gate open and RATES_FILE_URL defaulted to the in-repo workbook (local verification only)
node scripts/make-sample.mjs [origin]     # regenerate public/sample/report.json via a local, gate-open, no-KV dev server
node scripts/xlsx-cells.mjs show|set|diff # inspect / edit / compare workbook cells without a SheetJS round-trip (see "Current state")
node scripts/check-report-fit.mjs [origin] [--pdf dir]  # prove every report page fits its A4 sheet (sample, worst case, long scope); --pdf exports them
```

`.claude/launch.json` has two preview configs: `estates-ai-tool` (plain `npm run dev`, gated exactly like production — it needs KV and a real account) and `estates-ai-tool-open` (runs `scripts/dev-open.mjs`, every request is a local developer account). Use the second for browser verification. Next 16 refuses to start a second `next dev` in the same directory; if one is already running you will see "Another next dev server is already running" with its PID.

**Vitest** (`vitest.config.mjs` + `vitest.setup.mjs`) covers `lib/scopeExpr.js` (the quantity-rule language), `lib/nrmWorkbook.js` (loading, every rejection check, last-good fallback), `lib/scopeEngine.js` via `lib/__tests__/scopeScenarios.test.js` (the brief's seven test scenarios, each "Must see" an assertion), the v5.2 report output (`reportV52.test.js`), `lib/costCalculator.js`, `lib/programmeCalculator.js`, `lib/senseCheck.js`, `lib/prose.js`, `lib/reportShared.js`, `lib/labels.js`, `lib/pipeline.js`, `lib/questionSets.js` and `lib/siteContext.js` — the percentage-rule matcher and its trace, size-band boundaries, budget verdict (incl. VAT position), confidence grading, deterministic risk seeds (incl. the Q3.8 context seeds), the number-leak guard, programme float / FastTrack / procurement-table selection, calendar dates, the shared report strings, and per-project-type question visibility/options and the Section 1 higher-risk-building derivation (incl. its project-type gate and the Extension storeys/height distinction), including regression tests pinned to the exact "general pattern shadows specific" bugs found and fixed in this codebase's history. Tests that depend on a workbook addition not made yet **skip with a message** rather than fail (the Programme workbook's BS1/SV7 rows were added in v4.4, so the Gateway 2 test now runs). `budgetVerdict()`, `computeConfidence()` and `ensureSeedRisks()` are pure and run instantly; the cost side reads the NRM1 v5.x workbook **committed in the repository** (content v5.4) (`NRM1_Cost_Estimate_Tool_v5_2.xlsx` — `vitest.setup.mjs` forces `RATES_FILE_URL` to it, so cost tests are offline and pinned to the workbook the code was written against; set `RATES_FILE_URL_TEST` to test another issue), and since 24 September 2026 the programme tests read the committed `Estates_AI_Programme_v4_3.xlsx` the same way (`PROGRAMME_FILE_URL` may now be a local path; `PROGRAMME_FILE_URL_TEST` overrides).

`scripts/baseline.mjs` remains the tool for **"did this change move any number"**, across ~75 scenarios via snapshot diffing — see its own header. Its stored snapshots predate the v5 switch (which moved every number), so take a fresh "before" snapshot first. Vitest is for **"does this specific rule behave the way it's supposed to,"** with a readable assertion and failure message rather than an opaque diff. Use both: baseline.mjs before/after a change that must not move anything, Vitest for anything with a specific, statable rule.

"Verifying a change" means `npm run build`, `npm test`, and exercising the flow in the browser.

Environment is Windows. The **Bash tool (Git Bash) works** as of September 2026 for node/npm/sed/grep; the **PowerShell tool** is better for process inspection (`Get-Process`), since Git Bash mangles Windows-style `/FLAG` arguments. Python is not installed — use Node.js and the `xlsx` npm package for any spreadsheet scripting (read with `fs.readFileSync` + `XLSX.read(buf, { type: 'buffer' })`; the ESM build's `XLSX.readFile` has no `fs` wired in).

**`nutritrack/`, `portfolio-advisor/` and `Beach Game/`** are unrelated side projects that happen to live in this repo's root folder (untracked in git, not part of this app). `tsconfig.json` and `eslint.config.mjs` both explicitly exclude them — without that, a bare `npm run build` or `npm run lint` would try to typecheck/lint their unrelated dependency trees and fail. If either exclude list ever needs touching again, that's why it's there; don't remove it to "clean up" the config.

## What this app does

A gated Next.js (App Router) web tool that produces **UK RIBA Stage 0–1 feasibility reports** for capital works projects. A user fills a multi-step questionnaire; the app returns a costed, programmed feasibility report on screen and as a downloadable `.docx`.

## Core architectural rule: the AI never calculates a number

Report generation is split across **two requests**, not one — this is the fix for a structural reliability problem, not a style choice; see "Why two requests" below.

**Phase 1 — `app/api/generate-report/route.js`** (deterministic only, returns in a few seconds):

1. **`lib/costCalculator.js`** → deterministic NRM1 cost JSON (no AI).
2. **`lib/programmeCalculator.js`** → deterministic RIBA programme JSON (no AI).
3. Cost runs **three times**: once with `programmeWeeks = 0`, again after the programme is known (Inflation (F) and the long-programme Prelims (A) trigger depend on programme length), and a final time with `{ rangeGrade: confidence.score }` because the estimate range width is keyed to the confidence grade ('3. Settings' ▶ range_widths), which is only known after the sense check. The budget verdict is then recomputed from the final cost. **`lib/pipeline.js`'s `runDeterministicPipeline()`** is this exact sequence as one function; `/api/compare` uses it so scenarios can never disagree with the report.
4. **`lib/senseCheck.js`** → deterministic warnings + confidence grade (`computeConfidence` in `lib/prose.js`).
5. **`lib/kv.js`'s `createReport()`** → writes the record with `status: 'deterministic'`, TTL 24h, and returns `{ reportId, cost, programme, budget, confidence }`. No AI call has happened yet.

**Phase 2 — `app/api/reports/[id]/prose/route.js`** (AI only, called by `/report/[id]` immediately after Phase 1 returns, and again on every subsequent attempt):

6. **Two Claude API calls** (`requestProseHalf` in `lib/prose.js`) → **prose only**, forbidden from recalculating, plus deterministic risk-register **seeds** (which risks appear is decided in code; the AI writes their wording).
7. **`lib/kv.js`'s `finaliseReport()`** → writes the complete record, `status: 'complete'` — no TTL when it has an owner (else 90 days) — and pushes it onto the admin index and the owner's `user:<uid>:reports` list for the first time. (The `.docx` is no longer built here — `GET /api/reports/[id]/docx` builds it on download; see "Report output".)

**Never let the AI invent or alter a figure — and never let it invent a claim about what the tool's own automated checks found.** The system prompt (`AI_SYSTEM_PROMPT` in `lib/proseSchema.js`) has fifteen absolute rules (15, added with the report design system: never cite questionnaire question numbers; rule 8 no longer asks for them); rule 13 exists because the model once fabricated a plausible-sounding "the automated sense check raised a warning" claim about a percentage rule that was never actually flagged — see "Recent Work & Status." Since September 2026 the prompt is no longer the only enforcement: `assertNoLeakedFigures()` in `lib/prose.js` extracts every `£` figure and every "N weeks" from the model's output and rejects the half if any is not present in the prompt it was given (the prompt being the only legitimate source of a figure); the existing retry loop feeds the offending figure back. Restatements in another style (`£415k`, `£0.415m`) normalise to the same integer and pass; a changed or invented figure fails. **The shape of the AI text is fixed too:** `PROSE_LIMITS` in `lib/proseSchema.js` (exactly 5 key findings, exactly 5 next steps, exactly 3 procurement considerations, word ranges per slot, risk description ≤ 25 / mitigation ≤ 20 words…) drives the tool field descriptions, a LENGTH AND COUNT LIMITS block in both prompts, and `proseShapeProblems()` beside the number-leak guard. A miss goes back through the same retry loop; if still out after the last attempt the payload is kept (`trimToLimits()` drops extra items, never words) and a Sentry warning is sent — a report is never lost to a style rule. Retries cost AI calls, so when the model keeps missing a range, tighten its guidance wording before widening the limit (the limits protect the page layout).

### Why two requests

Both AI calls used to run inside the same request as the deterministic steps, sharing one 48s slice of the 60s function ceiling (see git history for the single-call and two-parallel-call attempts that preceded this — both failed for different reasons). That worked most of the time (~27s typical) but had **no room for a retry of a slow half**: one bad API moment and the whole report — deterministic data included — was lost after the AI call had already been paid for.

Splitting the AI phase into its own request removes the shared budget entirely. Phase 1 never touches the network and finishes in ~1s, so it cannot time out. Phase 2 gets a **fresh 60s** on every attempt — see "Resumability" below — so a slow API day costs extra round trips, not a lost report.

The two Claude calls inside Phase 2 (`PROSE_HALVES` in `lib/prose.js`) still run **sequentially, not in parallel**: two concurrent streams each get about half the token throughput, so parallel saves no wall clock and pushes the longer half past its timeout (measured: risk half 4,780 chars in 11.7s alone vs 25.7s alongside the other). The schema is split into two disjoint strict tools in `lib/proseSchema.js` — `PROSE_TOOL_NARRATIVE` (summary, findings, assumptions, cost/ROI narrative, constraints, next steps) and `PROSE_TOOL_RISK` (risk register + procurement) — merged by `finaliseProse()` into the same flat `aiProse` object the original single tool used, so `reportBuilder.js` and `ReportRenderer.jsx` are unchanged. **Keep the two schemas disjoint**: a key in both would make merge order significant. Both halves get an identical context prefix and differ only in field guidance, so neither depends on the other's output — where the narrative needs the headline risk or the procurement route it reads them from the deterministic seeds and programme data.

### Resumability

`/report/[id]` (`app/report/[id]/page.jsx`) is the driver. It loads the Phase 1 record, renders it immediately (pending placeholders for the AI-only sections — see `ReportRenderer.jsx`'s `isPending`/`PendingNote`), and calls `POST /api/reports/[id]/prose` repeatedly until `status` is `'complete'`. Each call:

- Skips any half already written (`lib/kv.js` sidecar keys `report:<id>:half:<narrative|risk>`).
- Claims a per-half KV lock (`nx`, 75s TTL) before attempting it, so two tabs open on the same report split the two halves between them rather than duplicating spend — never retried in a hot loop if another invocation already holds it.
- Checks the remaining budget against that half's own `minAttemptMs` (narrative 14s, risk 20s, from measured p50s) before starting — a half it can't finish this attempt simply isn't attempted, and the next call gets a fresh 60s.
- Finalises (builds the `.docx`, writes the final record) the moment both halves exist, however many calls that took.

A tab that opens a shared link to a report already mid-generation elsewhere does **not** also start calling `/prose` — it polls the cheap `/api/reports/[id]/status` instead, so every viewer's tab doesn't pile onto the same lock contention.

**This deployment is on Vercel Hobby: a hard 60s function ceiling.** `maxDuration = 60` on both routes. Do not raise it above 60 without moving to Pro/Fluid first: the platform kills the invocation regardless, and because the kill is external *none* of the error handling runs (no Sentry capture, no KV write, no JSON error) — Phase 2's resumability is what makes this survivable now (the next call just tries again), where it used to mean losing the whole report.

`/api/warm-prose` must warm **both** schemas — a first-ever compile of an unseen schema costs ~23s, and an un-warmed half pays it on the real call.

### No-KV fallback (local dev only)

If `KV_REST_API_URL`/`KV_REST_API_TOKEN` are unset, `createReport()` returns `false` and Phase 1 has no shared state channel for a second request to resume from — there'd be nowhere to put the deterministic record between the two phases. `generate-report` falls back to `lib/prose.js`'s `runProseSequential()`, reproducing the old single-invocation behaviour (both halves in one request, `status: 'complete'` in the response, no `reportId` persisted anywhere). This is why KV is not really optional in production despite the graceful-degradation wrapper in `lib/kv.js` — without it, every report reverts to the original one-shot-or-lose-it reliability profile this whole split exists to fix.

## The calculators are driven by remote Excel workbooks

Both `fetch` an `.xlsx` from a URL env var at request time, parse with SheetJS, cache in-module 10 min. (`RATES_FILE_URL` may also be a local path, for tests and `scripts/dev-open.mjs`; production always uses a URL.)

### costCalculator.js ← `RATES_FILE_URL` (NRM1 v5.x; committed content v5.4) — with lib/nrmWorkbook.js, lib/scopeEngine.js, lib/scopeExpr.js
The workbook is the app's brain: every scope item, option, rule, list, rate, ratio and percentage is read from it at runtime and **none is held in code**. Four modules:
- **`lib/nrmWorkbook.js`** (server-only) loads and checks the file. '2. Scope and Rates' columns are found by **header text** in the row starting "Rate key" (a trailing "(…)" note in a header is ignored); '3. Settings' tables by their **▶ markers**, each ending at the first blank row. **Check before use:** a file is rejected if a required sheet/table/header is missing, a formula cell was saved without a value (a tool that doesn't recalculate), any self-check on '1. Instructions' isn't 0, a quantity rule/condition uses an unknown name, ratio or `Q()` item, a project-type/building-use code isn't defined on Settings, a pricing type or 'When selected' effect type is unknown, or a required ▶ percentage_rules row / the ★ DEFAULT location is missing. A rejected update leaves the **last good version** in use (per instance) and is reported by `/api/rates-check` (`workbookStatus()`); with no good version yet the error propagates — no hard-coded fallback. `RATES_FILE_URL` may be a local path (tests, `scripts/dev-open.mjs`). The v4.5 workbook is recognised and rejected with a message saying so. `publicCatalogue()` is the same model with rates and price sources stripped — what `/api/scope-items` sends the questionnaire (options carry only `priceableFor` booleans per project-type code).
- **`lib/scopeExpr.js`** (pure) is the quantity-rule/condition language from '1. Instructions' — names (GIFA, STOREYS, FOOTPRINT, UPPER, EXT_AREA, DEMOLISHED, PT, USE, AGE, LEVEL), `r.ratio`, IF/ROUND/MAX/MIN/SQRT/`Q(S-0000)`, `=` with several codes meaning any, and `ENTER` (contagious through arithmetic; only the taken IF branch counts). Hand-written parser, **never `eval()`**; an unknown name throws at parse time (a new name is a code change by the workbook's own rules).
- **`lib/scopeEngine.js`** (pure, shared by the questionnaire and the cost engine, so the picker's "≈ 40 nr (estimated)" is the quantity priced) holds how the columns combine. Visibility = 'Shown on' holds the Q1.2 code (AUTO rows never shown). Building use never hides an item — tagged items (or ALL, or a use whose ▶ building_uses note says every item is listed) are listed first and can start ticked; the rest sit under "More items". **Rule order:** project type ('Pre-ticked on'), then level of intervention for types whose ▶ project_types row says so (RF, FO: 'Pre-ticked at intervention levels' on any option row, greyed while 'Available from intervention level' is higher), then 'Other conditions' (tick/untick — a tick only applies to an item the building use can start ticked), then 'When selected' as choices are made. Default option: pre-ticked at the chosen level, else 'Default option', else the first available; the user's choice (`q2_2_scopeOptions`) wins while valid. An option with no rate for the project type is left out of a drop-down, but an item with **no** rate at all still appears ('Shown on' decides) and prices as excluded "no rate" — a workbook gap shown, not buried (Solar PV / Battery storage on External works only were the example until workbook v5.3 filled them). Effects: SUGGEST ticks another item when this one is ticked; RATE bumps a target's **default** option one band (once however many sources); RISK and ASSUME are returned as `scopeEffects`. `migrateScopeCodes()` / `normaliseScopeAnswers()` translate v4.5 codes through 'Replaces old codes' (retired and auto codes dropped) so old drafts and KV reports still load and re-run.
- **`lib/costCalculator.js`** prices the resolved lines: **line = rate × quantity × location factor × band multiplier**, the last two only where 'Apply band + location factor' = Yes, no extent or intervention % on top. Rate = the option's row for the building use (else ALL) in the column ▶ project_types names for the project type × Q2.4 spec level. "Per component" (Other or mixed) reads the Rfb column and falls back to NB where that is zero (`rateFallbacks`) — the permanent design since the mixed area split was dropped, **deliberately not** the per-component split the Settings note describes. Band multiplier from ▶ intervention_levels, RF/FO only (1 otherwise). Modular (Group 2 header) multiplies the groups ▶ factors `modular_factor` names. ENTER, zero-quantity and no-rate lines go to `excludedNoQuantity` with a reason; not-offered/unavailable/unknown ticks to `adjustments`; the **reconciliation invariant** still throws if a ticked item is none of priced/excluded/adjusted. The AUTO row (builder's work) is added when a Group 5 line priced. **BCIS 12 lines (biodiversity net gain) are `belowLine`** — outside works/construction, no percentages on them, added to the project total as their own row; loose furniture (BCIS 4.1) is inside the total. `bcisTotals` splits every works line by its BCIS element shares. Each line carries `qtySource` ('estimate' | 'user'), `aiEstimate` (price source "AI estimate – verify"), `ratioEstimate` and `rateKey`; `code` is the Scope ID.
- **Every percentage is traced.** `evaluatePercentageRules` (exported) reads ▶ percentage_rules and returns `trace` alongside `percentages` — one `{ code, label: <condition text>, pct }` per fired rule, caps as a negative final entry — for the report's build-up table (`buildUpRows()` / `buildUpPromptLines()` in `lib/reportShared.js`). The "≥4 M&E items" rule reads the scope engine's Group 5 item count (`meItemCount`), not a count of codes (one v5.2 item, Heating, replaces three v4.5 codes).
- `vatPct` from the G row; contingency (H) fixed 5%; inflation (F) tender vs construction bands against component-specific spans (`constructionWeeks` param). The range comes from '3. Settings' ▶ range_widths (Grade · Low · High, added in workbook v5.3, illustrative values from the September spec); a grade with no row falls back to the legacy 0.89/1.11. The workbook has no rate base date, so `baseDate` is null ("the workbook issue date").
- `checkCondition()`'s matching order matters: several money bugs earlier in this codebase's history came from a *general* condition (e.g. "Full planning (3–4%, use 3.5%)") matching before a *more specific* one (e.g. a listed-building 4% row), because the general row's test matched a substring of the *answer* rather than requiring its own condition text. If you touch this matcher, add a Vitest regression case in `lib/__tests__/costCalculator.test.js` alongside the ones already there pinning this exact failure mode (they tick v4.5 codes on purpose, so they also exercise legacy-code translation).

### programmeCalculator.js ← `PROGRAMME_FILE_URL` (Programme v4.4)
**One unified 6-band size scheme (S1–S6)** for design *and* construction:
`sizeBand(gifa)`: <150→S1 · ≤250→S2 · ≤500→S3 · ≤1500→S4 · ≤3000→S5 · >3000→S6 (the `<`/`≤` boundaries are exact — 150 itself is S2, not S1; see `lib/__tests__/programmeCalculator.test.js` for the full boundary table).
- Sheet `Durations` is **ID-keyed** (DS2, GW, GW3, SV1–6, PL0–5, BC1–2, TN1–3); columns are `ID · Phase · Activity · S1_Lo · S1_Hi … S6_Lo · S6_Hi · Unit · Type · ParallelWith · ScaledByQ2.3 · Trigger · Notes`. Skip rows with blank ID/Type (banners).
- Sheet `Construction` matched by **Project Type name (col 1)**; bands `S1_Lo…S6_Hi`; `Handover` = CH1. `selectConstructionId()` (code, not workbook) maps project type + scope + spec level to the row ID actually used — and has no group-1 (substructure) test: New Build and Extension return earlier, and every other type had 1.1–1.4 hidden by `priceableFor()` until "Other or mixed" made them reachable (via `lib/projectTypes.js`'s new-build rate fallback), so a mixed project with foundations now falls through to `CF2` (Fit-Out — Basic/Cat A) for its construction duration. Known gap, not fixed: the Construction sheet has no mixed row to pick instead, and inventing one in code would violate the no-numbers-in-code rule (see the `hasFabric` fallthrough comment in the function, and the design spec's "Not in this slice" table).
- **Scope hand-off:** `calculateProgramme(answers, costMid, { scope: cost.scopeSummary })`. This engine's scope tests are written against v4.5 codes and keywords ('0.2' demolition, '7.1' structural, lab/clinical names), so it receives the v4.5 codes each ticked v5.2 item replaces plus the item names (`tokens`) and a true Group 5 count (`meItemCount`). Verified identical on the sample report (124 weeks, same construction row). Roof scope for the SV7 trigger is v4.5 2.3/7.2 or a v5 item named for a roof. The summary also carries `interventionLevel` — the level the cost was priced at, **null where the project type doesn't use one** — and the programme reads that, not the raw answer, for the Q23 design multiplier and `selectConstructionId()`, so a stale level (an old Extension draft) can't shorten the design stages while the cost ignores it.
- Sheet `Modifiers` owns the **Q2.3 design multiplier** (`Q23-1..Q23-4`, `Q23-NB`), occupation (`OCC`), phasing (`PH-1`), funding governance (`FN-1`), access (`ACC-1`/`ACC-2`), hard deadline (`DL-1`), float (`PROG-FLOAT`).
- Sheet `FastTrack` — FT1 (start point, Q4.5) is applied to the headline; FT2–FT10 are evaluated deterministically (`FT_TRIGGERS` in `calculateProgramme`) and returned as `fastTrackOptions` — **offered in the report and prompt, never applied**.
- `PROG-FLOAT` (`+1 wk per 13 wks`) **is applied**: `totalWeeks` is the with-float headline, `totalWeeksBestCase` the critical path without it, `floatWeeks` the difference, plus a `Programme float` stage row. Inflation bands and the target-date check see the with-float figure.
- Optional sheet **`Procurement`** (decision table: primary priority = `q4_4_priorities[0]` × cost mid band × design stage → route, contract form, tender type, tender ID TN1–3, design responsibility, rationale; first row wins). Absent → the original value-only route/contract logic, kept verbatim as the fallback. Output: `tenderType`, `designResponsibility`, `procurementRationale`, `procurementSource`.
- Optional Durations rows **`SV7`** (ecology/bat survey — Q3.8 ecology option, or roof scope on a pre-2000 building; parallel) and **`BS1`** (Building Safety Regulator Gateway 2 — `isHigherRiskBuilding()` in `lib/siteContext.js`, derived from Section 1 (Q1.2a storeys / Q1.6 height / Q1.3 building use) rather than a Q3.8 tick, with the legacy Q3.8 tick still honoured for reports generated before that change; sequential between Stage 4/BC and tender, `bsaGatewayWeeks`). No row → no stage, no duration: the code never invents one.
- **Calendar dates** (Q4.0 `q4_0_startDate`, else the generation date with `startDateAssumed: true`): every stage gets `startWeek/endWeek/startDate/endDate` (parallel stages anchored to the stage they run alongside), milestones read `Week N (12 Mar 2027): …`, and the target-date check measures from the start date, not `new Date()`.
- Surveys run **parallel** with design; planning **gates** Stage 4 (carry the overrun); BC parallel with Stage 4 (carry overrun). `GW3` only for S3+ (GIFA > 250).

Changing a rate or duration means **editing the workbook, not the code**. The design multiplier the programme uses lives only in Programme Modifiers; NRM1's ▶ intervention_levels has its own "Design duration multiplier" column, which nothing reads (the cost engine takes only the band multiplier from that table).

### senseCheck.js
Runs after both calculators, before the AI call. Reads '3. Settings' ▶ benchmarks from the NRM1 workbook by project-type code × building-use code (cost benchmarks live in the workbook, not in code). Also exports `budgetVerdict(answers, cost)` — a deterministic comparison of Q4.3 stated budget (incl. fees + VAT) against the gross estimate range; result goes into the AI prompt and the report payload. Programme benchmarks (wide size-band envelopes) remain in code because there is no matching programme-benchmark sheet.

Exactly nine warning codes exist: `COST_LOW`, `COST_HIGH`, `POSTCODE_UNMATCHED`, `RULE_UNMATCHED`, `BENCHMARK_MISSING` and `RATE_FALLBACK` (all three marked `internal: true` — workbook/rate diagnostics for maintainers, never for the client; `RATE_FALLBACK` names the element codes that priced from the new-build column per `cost.rateFallbacks`, a deliberate product decision not to disclose the mixed rate basis to the client), `PROG_SHORT`, `PROG_LONG`, `BUDGET_SHORTFALL`. `clientWarnings = warnings.filter(w => !w.internal)` is what reaches the AI prompt and the client-facing report. **`BENCHMARK_MISSING` is the one internal code with a client-visible consequence**, and deliberately so: ▶ benchmarks having no row for a project type / building use combination is our gap, not the client's, so the warning itself stays internal — but the cost-per-m² check did not run, so the estimate has not been independently validated and cannot honestly be graded A. `runSenseCheck` therefore returns `benchmarkChecked`, and `computeConfidence()` in `lib/prose.js` reads it directly (not via `clientWarnings`) and adds a deficiency worded as a limitation of the estimate. Only an explicit `false` penalises, so a sense check from before this field existed — or a call site that passes none — grades exactly as it did before. This exists because a 95 m² coffee-shop fit-out priced at £189/m² was graded "A — High Confidence": with no band there was no `COST_LOW`, and the miss was logged with `console.log` to a server nobody reads. **An absent check must never be indistinguishable from a passed one.** If you're ever debugging a risk-register entry that claims "the automated sense check raised a warning" about something not on this list, the AI fabricated it — see "SYSTEM DIAGNOSTICS" in `AI_SYSTEM_PROMPT` and "Recent Work & Status" below. **`RATE_FALLBACK` currently reaches only the generating invocation's own console** — `serializeCost()` in `app/api/generate-report/route.js` does not carry `cost.rateFallbacks` into the KV record, so it is not persisted and does not show up in the admin view; a maintainer who wants to know which elements fell back on a past report has to regenerate it and read the server log.

## Report output: built in code, not from the template file

**Report design system (September 2026, branch `feat/report-design-system`).** The report is a fixed set of **A4 pages**, identical on screen, in the PDF and in Word. Spec: `docs/superpowers/specs/2026-09-23-report-design-system-design.md`; approved look: `docs/superpowers/specs/2026-09-24-report-template-approved.html`; plan: `docs/superpowers/plans/2026-09-24-report-design-system.md`.

- **`lib/reportStyle.js`** is the only source of the look: `BRAND` (the placeholder name "Estates AI", strapline, web and **placeholder** contact details — change them here and every page of every output follows), `COLOURS`, the six body type sizes (`TYPE`, pt) plus `COVER_TYPE`, `PAGE` geometry (794 × 1123 px; 1 px = 15 DXA), `TABLES` column recipes and `LIMITS`. Typeface is IBM Plex Sans / Mono everywhere.
- **`lib/reportContent.js`** decides everything derived: rounding (`money()` — £100 steps under a £100k project, £1,000 above; the cover's compact `£1.60m`), titles, `cleanReportText()` (strips "(Q3.5)"-style references), the risk register (`prepareRisks()` — de-duplicated, High first, ten at most, renumbered R01…; `riskTableMode()` compact for pre-cap text), the programme page (six milestones, one-line bar, folded detail rows, narrative), the cost pages, scope lines, and the **page map** (`buildPageMap()`): cover · summary · scope (always page 3) · risk · programme · two cost pages · Financial Case / Procurement / Constraints sharing pages (`layoutLateSections()` — split at the middle when both fit a half, else the second follows) · Next Steps + contact (always last) · Appendix A for scopes longer than one works page.
- **HTML (screen + PDF):** `app/report/doc/*` — one component per page kind, styles in `report.css` using only `var(--r-…)` from `cssVariables()`. `ReportRenderer.jsx` keeps only the toolbar, alerts, compare panel, CTA and feedback modal. On screen the sheets scale to fit the width (pinch-zoom on phones).
- **Word:** `lib/docx/*` (`primitives.js` is the only place a `TextRun` is built; `pages.js` one builder per page kind) assembled by `lib/reportBuilder.js`. IBM Plex is embedded (`assets/fonts/`, SIL OFL; converted losslessly from IBM's WOFF). Word-specific rules learnt the hard way: embedded font parts must have no spaces in their names (`fixEmbeddedFontPartNames()` — else Word calls the file corrupt); a table cell must end with a paragraph; tables need explicit no-borders and fixed layout; the SemiBold TTF's family name is `IBM Plex Sans SmBld`. Check a change in real Word: build the .docx and open it via Word COM (`Word.Application` → `Documents.Open` → `SaveAs(..., 17)` for a PDF, `ComputeStatistics(2)` for the page count).
- **The .docx is built on download** by `GET /api/reports/[id]/docx` — no longer at finalise, no longer stored in KV (the embedded fonts would push records towards the size limit). The no-KV local path still returns it inline. `next.config.ts` traces `assets/fonts` into that route.
- **Guard:** `lib/__tests__/reportStyleGuard.test.js` fails on any hex colour, font-size literal or banned font name in `app/report/doc/**`, `lib/docx/**` or `lib/reportBuilder.js`.
- **Fit check:** `node scripts/check-report-fit.mjs [origin] [--pdf dir]` against a gate-open dev server renders the dev-only `/report-fixture/{sample,worst,long-scope}` (fixtures in `lib/reportFixtures.js`; `worst` fills every prose slot to its `PROSE_LIMITS` maximum) and fails if any page's content reaches its footer, a half-page slot overflows, Scope isn't page 3 or Next Steps isn't last. Tighten a content rule (a `LIMITS`/`PROSE_LIMITS` value), never the page.

**PDF export** (`app/api/report-pdf/[id]/route.js`) renders the *live* `/report/[id]?pdf=1` page via Puppeteer with zero margins and `preferCSSPageSize`, after `document.fonts.ready`; each A4 page carries its own margins, running header and "Page N of M". The download button no longer falls back silently to browser print when the route fails — it shows an error.

## Data flow & key conventions

- Answer keys use `q<section>_<index>_<name>` and must match **Questionnaire v7** exactly. There is no single canonical key-set list in the codebase — `app/questionnaire/page.jsx` is the source of truth for which keys exist and how, but they're declared where each question is rendered, not collected in one place at the top of the file. Notably: design stage = `q4_5_designStage`; phasing = `q4_6_phasing`; funding = `q4_7_funding`; BREEAM is in `q2_5_standards`; scope quantities are `q2_2_quantities` keyed by rate key (the v4.5-era `q1_5_*` PV/BESS/lift/EV quantity keys are no longer read). There is **no** `q4_8`/`q4_9`. **Added September 2026:** `q3_8_siteContext` (multi-select; option strings and the substring keys the engines test live in `lib/siteContext.js` — conservation area, party wall, ecological features) and `q4_0_startDate`. `q4_4_priorities` is capped at two in the UI and the first tick is the primary priority the Procurement sheet reads. `q1_2_storeys` now goes up to `7` ("7 or more"). **Added by the question-gating slice (September 2026):** `q1_6_heightOver18m` (Q1.6, shown only for New Build/Refurbishment/Extension at 5+ storeys — `showsHeightQuestion()` in `lib/questionSets.js`); Q3.8 no longer has a "higher-risk building" option — that status is now derived from Section 1, see `isHigherRiskBuilding()` in `lib/siteContext.js`. Which questions each project type asks or offers which options is centralised in `lib/questionSets.js` (`isQuestionShown()`, `knownIssuesFor()`, `surveysFor()`, `occupationCopyFor()`, `showsHeightQuestion()`) rather than scattered inline conditions — and, since the honest-progress slice (September 2026), so is which questions are *required*: `isQuestionRequired()`, the per-section inventory `QUESTIONS_BY_SECTION`, and the counting helpers `sectionCounts()`, `unansweredRequired()` and `progressPercent()` all live there too, and the questionnaire renders its progress bar, header counts and status line from them rather than hand-written conditions. Sections 3 and 4 now have client-side validation (`validateSection()` in `app/questionnaire/page.jsx`) where they previously had none; Q5 (financial case) and Q6.1 (report instructions) sit behind two collapsed disclosures at the end of Section 4, open by default only when a returning draft already holds an answer there.
- **Visible question numbers are not the key names, and two of them deliberately differ.** `q4_0_startDate` is labelled **Q4.2** and rendered after Q4.1, taking the number freed when the dead budget-gate question was removed, so step 4 ascends. `q6_2_instructions` is labelled **Q6.1** now that the sections question is gone. Renumbering anything else is not free: the NRM1 workbook's Tab 3 conditions quote these numbers verbatim ("Q3.6 = Fully occupied throughout", "Q4.1 = Hard deadline", "Q2.5 includes BREEAM") and the report's build-up table prints that text, so a UI-only renumber puts the form and the report out of step. The 2.2/2.3 numbering now follows page order: **Q2.2 Level of intervention** (keyed `q2_3_interventionLevel`) then **Q2.3 Scope of works** (keyed `q2_2_scopeItems`) — labels only, keys unchanged. Workbook v5.3's percentage-rule text reads "Q2.3 has ≥4 M&E items"; the matcher keys on "≥4 m&e", never the question number.
- **NRM1 v5 scope answer keys** (`app/questionnaire/ScopePicker.jsx`): `q2_2_scopeItems` = ticked **Scope IDs** (S-0039); `q2_2_scopeOptions` = `{ scopeId: [rate keys] }`, only choices the user made (defaults are recomputed, so they follow the level); `q2_2_quantities` = `{ rate key: qty }`, only figures the user gave ("I know this"); `q2_2_extArea` (Group 8 header — on External works only the Q1.5 answer IS the external area and GIFA is 0); `q2_2_constructionMethod` (Group 2 header, Traditional/Modular). `q2_2_wiring` is retired. Nothing starts ticked: "Use typical scope" and group/section ticks apply the defaults; unticking a group/section unticks everything under it. Draft schema v5; a v4 draft is **migrated** (codes translated when the catalogue arrives, in the fetch callback), not discarded. Level of intervention is asked only where ▶ project_types says so (Refurbishment, Fit-out — **no longer Extension**, decision 2 of the brief). Q1.2, Q1.3, the level list and Q2.4's levels are rendered from '3. Settings' (a static copy of the same labels shows only until the catalogue loads).
- **Two questions were removed in the September 2026 question review.** There is no VAT-position question: it only ever changed the wording of the budget comparison, never a cost, so `budgetVerdict()` grosses up by the full workbook VAT rate. There is no "which report sections" question: it asked the user to decide before seeing a report, and the two genuinely conditional sections already self-hide. `resolveSectionFlags()` keeps both legacy keys alive purely for reports sitting in KV, which lasts 90 days.
- **Q3.3 surveys: blank means "None" to the engines.** `computeConfidence()` has always treated an empty answer as no surveys; `checkCondition()` now does too, so a report can no longer say "Grade C, no surveys commissioned" while pricing the project as though surveys were in hand. Any scenario that leaves surveys blank carries the workbook's +2pp risk (and +0.5pp developer costs where a planning route applies). The question itself is no longer optional, though: the honest-progress slice made Q3.3 one of seven questions the FORM now requires an explicit answer to (an empty array no longer submits) — "blank" in the paragraph above describes what the engines do with an answer that predates that change, or one from the no-KV/API path that bypasses the form's own validation, not something the questionnaire itself still permits.
- **Persistence:** Vercel KV (`lib/kv.js`, key `report:<id>`). 24h TTL while `status: 'deterministic'`, kept with no expiry at finalise when it has an owner (90 days when it has none) — see "Resumability" above for the full two-phase record lifecycle (`createReport` → `saveProseHalf` × 2 → `finaliseReport`). Absent KV vars degrade to the no-KV fallback described above rather than failing outright, but production needs KV configured for the reliability split to actually apply. Canonical shareable URL is `/report/[id]`; `/report` is a legacy entry point that redirects to it.
- **Access control:** `proxy.ts` (Next 16's `middleware.ts` replacement) requires a signed-in user (`projento_session`, see "Current state" and `lib/auth.js`) on the questionnaire, reports and every costly API, and `estate_admin` (HMAC via `lib/cookieAuth.js`) against `ADMIN_CODE` for `/api/admin/*`. Both **fail closed in production**. `POST /api/logout` clears the cookies in this browser; a password change or a disabled account signs out every device.
- **Rate limiting:** `lib/rateLimit.js` (`@upstash/ratelimit` on the same KV store — no separate env var needed) sliding-window limits five routes: `check-access` (10/10m), `admin-login` (5/10m), `generate-report` (30/10m), `prose` (60/10m — generous, since the resumability driver legitimately calls it several times per report), `feedback` (10/1h). **Fails open**, not closed, on infra failure — an infra hiccup degrades to "no rate limiting" rather than "nobody can use the app," matching `lib/kv.js`'s own graceful-degradation posture.
- **PDF export:** `app/api/report-pdf/[id]/route.js` — see "Report output" above. Guards against rendering a still-generating report (`status !== 'complete'` → 409).
- **Scope items API:** `app/api/scope-items/route.js` returns `{ catalogue, bcisRegions }` — `publicCatalogue()` (items, options, rules, Settings lists, no rates) and the ▶ location_factors regions.
- **Scenario comparison:** `POST /api/compare { answers, axis: 'spec' | 'intervention' }` re-runs `runDeterministicPipeline` per variant and returns headline figures only. Rendered by `ComparePanel` in `ReportRenderer.jsx` — **screen only**, never in the PDF/.docx (it is a what-if tool, not the report of record). Protected + rate-limited (30/10m).
- **Project type (Q1.2):** seven options on a single fabric axis (New Build, Extension, Refurbishment, Fit-out, External works only, Demolition only, Other or mixed), now defined on '3. Settings' ▶ project_types with their codes (NB EX RF FO EW DM OM). `lib/projectTypes.js` keeps the help text and `rateFamilyFor()`/`usesRateFallback()`; its `VISIBLE_GROUPS`/`priceableFor()` are v4.5 leftovers the picker no longer uses ('Shown on' replaced them). "Other or mixed" is the one type that can trigger the new-build rate fallback (`cost.rateFallbacks` / `RATE_FALLBACK`).
- **AI scope suggestion:** `POST /api/suggest-scope { objective, projectType, buildingUse, interventionLevel, buildingAge, storeys }` — Haiku with a strict tool whose `code` enum is exactly the Scope IDs the picker offers, filtered by the same `lib/scopeEngine.js` tests (no hand-duplicated tier or folded-code lists any more), tagged-for-this-use items first. The model proposes; the user edits; the engine prices. Protected + rate-limited (20/10m).
- **Sample report:** `/sample` (public, outside the sign-in gate) renders `public/sample/report.json` through `ReportRenderer` with the `sample` prop (actions hidden, banner shown). Regenerate with `scripts/make-sample.mjs` whenever the report structure changes.
- **Shared report text:** `lib/reportShared.js` is imported by *both* renderers and the prompt builder — build-up rows, Estimate Basis sentences (range, base date, rate sources), programme headline, FastTrack lines, section flags, date formatting, and the v5 line wording: `lineBasis()` ("≈ 60 m² · estimated" / "12 nr · your figure" / "† unverified rate"), `quantityBasisSentence()`, `unverifiedRatesSentence()`, `bcisGroupSentence()`, `belowLineRows()` (the BCIS 12 row in the project cost table), `scopeAssumptionLines()`, `bandFactorSentence()`, `groupHeading()`. Every v5 helper returns ''/null/[] for a pre-v5 KV report, so old reports render exactly as they did (`lib/__tests__/reportV52.test.js`). Put anything both renderers must say identically there, never in either renderer. It must stay free of React and node-only imports (a `'use client'` file cannot be imported by `reportBuilder.js`, which runs on the server).
- **Building use matching:** in v5 the scope engine matches building-use **codes** from '3. Settings' (`isRelevant()` in `lib/scopeEngine.js`). `lib/buildingUse.js`'s `matchesBuildingUse()` / `BUILDING_USE_TAGS` are v4.5 leftovers; only `countMeItems()` is still called, as the programme engine's fallback when no scope summary is passed.
- **Feedback:** `app/api/feedback/route.js` is **POST-only** — the GET that used to read entries back via `?key=<ACCESS_CODE>` in the query string was removed (a secret in a URL lands in logs/history/Referer, and it was redundant with `/api/admin/overview`, which serves the same `listFeedback()` data behind the proper cookie).
- **Error boundaries:** `app/error.jsx` (any page under the root layout — preserves fonts/header, unlike `app/global-error.tsx` which only fires for a root-layout crash and discards the whole document), `app/questionnaire/error.jsx` (reassures the user their draft is still saved locally), `app/not-found.tsx`. All three capture to Sentry when `NEXT_PUBLIC_SENTRY_DSN` is set.
- **Questionnaire draft storage:** `localStorage` key `estatesAI_v4_answers` (name kept), wrapped as `{ schemaVersion, answers }` (`STORAGE_SCHEMA_VERSION` = 5 in `app/questionnaire/page.jsx`). A v4 draft is accepted and migrated (`migrateDraftScope()`, run when the catalogue arrives); any other version mismatch, malformed JSON, or non-object payload is discarded rather than rehydrated. Every read/write is guarded — an unguarded `localStorage.setItem` throwing (Safari private browsing, a full quota) inside a `useEffect` used to white-screen the whole page on the first keystroke.

## UI design system

All shared primitives live in `app/components/ui.jsx`: `Button`, `Badge`, `Card`, `Stat`, `SectionHeader`, `Field`, `Input`, `Textarea`, `Select`, `ControlGroup`, `ProgressBar`, `Rag`. These are class-driven — the classes are defined in `app/globals.css`. The questionnaire (`app/questionnaire/page.jsx`) has its own parallel set (`RadioGroup`, `CheckboxGroup`, `TextInput`, etc.) tuned for the multi-step form rather than the admin dashboard.

**Fonts** (loaded in `app/layout.tsx` via `next/font/google`):
- `--font-display` → Playfair Display (serif) — headings, decorative
- `--font-body` → DM Sans — body text, UI copy
- `--font-mono` → DM Mono — labels, eyebrows, code

**Key CSS tokens** (defined in `:root` in `globals.css`):
- `--navy` / `--ink` — primary brand navy `#1A2E4A`
- `--amber` / `--accent` — warm amber `#9D6B15` (darkened from the original `#C4861A` for WCAG AA contrast — see "Recent Work & Status"), used sparingly
- `--bg` / `--tint` / `--tint-2` — warm off-white background family
- `--border` / `--border-2` — warm neutral borders
- `--text-mute` / `--text-muted` — `#6D7182` (also darkened for contrast; was `#8B8FA0`)

There is no `--blue` token any more — it was a legacy alias for `--navy` with identical hex values; all 23 usages were replaced with `var(--navy)` directly and the dead token removed. If you see `var(--blue)` anywhere, that's a sign of an un-merged branch or a copy-pasted snippet from before this cleanup — replace it with `var(--navy)`.

**Key CSS utility classes**: `.btn-primary` (navy gradient), `.btn-accent` (amber gradient), `.btn-ghost`, `.panel-dark` (navy gradient panel), `.card` / `.card.lift`, `.eyebrow` (mono uppercase amber label), `.display` (Playfair), `.mono` (DM Mono), `.rise` / `.rise-1`–`.rise-4` (staggered fade-up entrance animation).

### Accessibility patterns established

These are the patterns to follow for any *new* interactive control — see "Recent Work & Status" for what's already been brought up to this standard and what hasn't:

- **A custom single-choice control** (radio-button-shaped `<button>`s, not native `<input type="radio">`) needs `role="radiogroup"` on the container, `role="radio"` + `aria-checked` on each option, and **roving tabindex** (one tab stop — the checked option, or the first if none is checked — with Arrow keys moving *and selecting*). See `RadioGroup` in `app/questionnaire/page.jsx`.
- **A custom multi-select** (independent toggle buttons) needs `role="group"` + `role="checkbox"`/`aria-checked` per option, but keeps native per-button tab stops — each is an independent on/off, not a set. See `CheckboxGroup`.
- **A custom checkbox built from a visually-hidden native `<input>`** (a real `<input type="checkbox">` at `opacity: 0, width: 1, height: 1` behind a custom-drawn box, so the screen-reader semantics are free but the browser's default focus ring lands on an invisible 1×1 box) needs `:focus-within` on the *visible* wrapping element to redraw the ring somewhere a sighted keyboard user can see it. See `.scope-tile:focus-within` in `globals.css` (from the v4.5 tile picker; the v5 `ScopePicker.jsx` uses visible native checkboxes with real `<label htmlFor>`s instead).
- **A collapsible section header** must be a real `<button>` (or have `role="button" tabIndex={0}` plus Enter/Space handling) with `aria-expanded`, never a bare `<div onClick>`. See the group "Show items / Hide items" button and the "I know this" toggle in `app/questionnaire/ScopePicker.jsx`.
- **A modal** needs `role="dialog"`, `aria-modal="true"`, `aria-labelledby` pointing at its visible title, and an Escape-key handler — clicking the backdrop or a Cancel button isn't enough for a keyboard user who has already tabbed inside. See `FeedbackModal` in `ReportRenderer.jsx`.
- **Dynamic status/error content** (a loading state, a submit error, a "still generating" banner) needs `role="status"`/`aria-live="polite"` or `role="alert"`/`aria-live="assertive"` so a screen reader announces it without the user having to go find it.
- **Any new colour** used for text should be checked against its background with the WCAG contrast formula before use — `--amber`, `--amber-deep`, `--text-mute`/`--text-muted`, and ten scattered inline greys (`#888`, `#999`, `#9CA3AF`) all failed AA (as low as 2.5:1) before this pass; the ones fixed now sit at 4.6–4.8:1.

## Environment variables

| Var | Purpose | If unset |
|---|---|---|
| `AI_API_KEY` | Anthropic key for the prose call | step 3 fails |
| `RATES_FILE_URL` | NRM1 **v5.x** rates workbook (a URL, or a local path for dev/tests; the v4.5 file is rejected). Production must point at `NRM1_Cost_Estimate_Tool_v5_2.xlsx` once PR #12 is merged | cost calc throws |
| `PROGRAMME_FILE_URL` | Programme v4.4 workbook (a URL, or a local path for dev/tests) | programme calc throws |
| `ADMIN_CODE` | Admin-area code for `/admin` + `/api/admin/*` (`estate_admin` cookie) — the admin invites users there | fails closed in production; open in dev |
| `RESEND_API_KEY` / `EMAIL_FROM` | Invitation and password-reset emails (`lib/email.js`); `EMAIL_FROM` on a domain verified in Resend | no emails — the admin copies each link from the Users panel; "forgot password" says to ask the admin |
| `APP_ORIGIN` | Origin used in emailed links (e.g. `https://www.projento.co.uk`) | Vercel's production URL |
| `AUTH_OPEN` | `1` = no sign-in, every request is a local developer account (set by `scripts/dev-open.mjs`) | ignored in production |
| `COOKIE_SECRET` | HMAC key for the session and admin cookies (`lib/session.js`, `lib/cookieAuth.js`) | production: nobody can sign in (fails closed); dev: a fixed development key |
| `KV_REST_API_URL` / `KV_REST_API_TOKEN` | Vercel KV (Upstash Redis) — accounts, persistence **and** rate limiting (`lib/rateLimit.js` reuses the same store; no separate rate-limit env var) | nobody can sign in (use `AUTH_OPEN` locally); persistence disabled; rate limiting fails open |
| `NEXT_PUBLIC_SENTRY_DSN` | Sentry error capture (`sentry.*.config.ts`, `instrumentation-client.ts`, `app/error.jsx` and friends) | error capture disabled (no-op) |

**Security:** `AI_API_KEY` never in committed code or output; read inside the request handler, BOM-stripped. `COOKIE_SECRET` must be a cryptographically random string (≥ 32 chars); generate once with `openssl rand -hex 32`. Sentry events are scrubbed by `lib/sentryScrub.js` before leaving the process — never send raw `answers`.

**`xlsx` is not installed from the npm registry.** `package.json` pins it to a SheetJS CDN tarball (`https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz`) because the npm-published `xlsx@0.18.5` has two unfixed high-severity CVEs (prototype pollution, ReDoS) that SheetJS only patches on their own CDN. `npm install` needs network access to `cdn.sheetjs.com` to work, and bumping this dependency means changing that URL to a newer SheetJS release, not `npm update`.

## Health check

`/api/rates-check` must confirm **both** workbooks load: `ratesOk` + a sample rate (Wall finishes, Rfb Std), and `programmeOk` + a real sample duration (DS2 S3 mid from the Durations sheet). For NRM1 it also reports `workbook` (version in use, and any **rejected** newer upload with its problems — the admin warning), and `gaps`: items shown on a project type with no rate for it, project types with nothing pre-ticked, and whether the optional ▶ range_widths table exists. Use it after every workbook edit. Gated by sign-in (or the admin cookie) and rate-limited like every other AI/workbook-touching route.

## Deployment

Hosted on **Vercel**. `npm run build` is the gate. Both `*_FILE_URL` workbooks must be reachable from the deployment. Run `npm audit` periodically — it had never been run before September 2026, when it turned up a critical Next.js RCE sitting in production undetected (see "Recent Work & Status"); it currently reports 0 vulnerabilities.

## Frontend Design Rules

When building or modifying any UI component, page, or interface in this app:

- **Aesthetic direction:** The tone is authoritative, precise, and refined — this is a professional capital works tool, not a consumer product.
- **Typography:** Use the established font trio (Playfair Display / DM Sans / DM Mono). Never introduce Inter, Roboto, Arial, or system-ui for new UI work. (`global-error.tsx` is the one deliberate exception — see its own comment: it's the last-resort boundary for a crash in the root layout itself, which is what loads the real fonts, so it can't safely depend on them.)
- **Colour:** Work from the established palette (`--navy`, `--amber`). Amber is a *sparingly used* accent — it should never dominate a surface. Check any new text colour against WCAG AA before using it (see "Accessibility patterns established" above) — several of the original palette's colours didn't pass and have since been darkened.
- **Motion:** CSS-only animations preferred. Avoid heavy JS animation libraries.
- **Backgrounds:** Never flat white. Use `--bg`, `--tint`, `--tint-2`, or the `panel-dark` class.
- **Production-grade only:** No placeholder content, no lorem ipsum, no half-built components committed to the repo.
- **Responsive:** Grids sized with `minmax(Npx, 1fr)` should use `minmax(min(Npx, 100%), 1fr)` instead — a bare `minmax(240px, 1fr)` doesn't shrink below 240px even when the viewport itself is narrower, which overflowed the page horizontally at ~320px on the scope picker and three other grids before this fix.

---

## Recent Work & Status

*(September 2026)* The app's core questionnaire → calculators → AI prose → report pipeline was already functionally correct when this round of work began. What follows was a five-phase reliability, security, correctness and accessibility pass (Part A of the working plan), plus a separate questionnaire UX reduction (Part B), plus a couple of bugs found by reviewing a real generated report afterward. Everything below is **done and verified** (build + lint + Vitest + a live regenerated report, not just read back) unless marked otherwise.

**The headline problem, and its fix.** Report generation failed close to 100% of the time on Vercel Hobby's 60-second function ceiling, because one request tried to do the deterministic maths *and* two sequential AI calls inside a single shared budget — a retry of a slow AI half had nowhere to come from. The fix (Phase 2, "Core architectural rule" above) splits generation into two requests: a deterministic phase that can't time out because it never touches the network, and an AI phase that gets a **fresh** 60 seconds on every attempt via a resumable, lock-protected KV record. This is the single most consequential change in this body of work — verified live against the real Anthropic API and the real database: deterministic phase ~1s, and a report that couldn't previously survive one slow API moment now just costs an extra round trip.

**Phase 1 — stop the bleeding.** Fixed a `maxDuration` mismatch that was silently killing invocations with no error handling at all; closed an admin-API hole that failed *open* when `ADMIN_CODE` was unset in production; gated two previously-open endpoints (`/api/warm-prose`, `/api/rates-check`) that were each a cheap way to burn API credit or exhaust function concurrency; scrubbed full questionnaire `answers` out of Sentry error payloads (`lib/sentryScrub.js`); wired up client-side Sentry, which had never actually been initialised (`instrumentation-client.ts` didn't exist); fixed `next.config.ts`'s dual `module.exports`/`export default` that made the entire Sentry build plugin dead code.

**Phase 2 — the two-phase split.** Covered above. Also: `lib/prose.js` was extracted from the route handler so both the resumable Phase 2 route and a same-request dev fallback (no KV configured) can share it.

**Phase 3 — correctness.** A `/report/[id]` page could silently render one project's costs against a *different* project's answers if the user had gone back to the questionnaire and started editing a second report without submitting it — fixed by using exactly what was submitted rather than re-merging the live localStorage draft. A programme with construction stages but no milestones showed its Gantt chart in the `.docx` but not on the web report (a nesting bug — the two now share one condition). The questionnaire submit path had three separate ways to show a false "network error" for a report that had actually generated successfully. `localStorage` reads/writes were unguarded, version-less, and could white-screen the app; there was no way to start a fresh report without manually clearing browser storage, and no error boundary anywhere except the root-layout-nuking `global-error.tsx`. All fixed; see "Data flow & key conventions" above for the current shape.

**Phase 4 — hardening.** Installing a rate limiter triggered `npm audit` for what was apparently the first time on this project, surfacing a **critical Next.js RCE** plus three riders (PostCSS, sharp) — fixed by bumping 16.2.6→16.3.5 (same major/minor line, not a breaking jump), confirmed with a full rebuild, relint, and a live end-to-end generation on the new version. Migrated `xlsx` off the abandoned, CVE-carrying npm registry version onto SheetJS's own patched CDN build. Added rate limiting to the five routes that needed it (see "Data flow & key conventions"). Removed the `/api/feedback` GET that leaked its auth key into URLs/logs. Added a logout route (there was previously no way to revoke either 30-day cookie). Permanently fixed the build/lint gate, which the three stray sibling projects in the repo root had been silently breaking. Added a real Vitest suite (33 tests) as a complement to the existing `scripts/baseline.mjs` snapshot harness — see "Commands" above for when to use which.

**Phase 5 — accessibility.** Brought the questionnaire's custom `RadioGroup`/`CheckboxGroup` (previously plain `<button>`s with zero ARIA semantics) up to full radiogroup/checkbox-group patterns including roving tabindex and arrow-key navigation; fixed a scope-picker collapsible header that was a keyboard-inoperable `<div onClick>`; fixed an invisible focus ring on the scope tiles' hidden native checkboxes; added dialog semantics and Escape-to-close to the feedback modal; added live regions to error/status banners; fixed three colour tokens and ten scattered inline greys that failed WCAG AA contrast; removed the dead `--blue` legacy token entirely; fixed a horizontal-overflow bug on four grids at narrow viewports. All verified live in a real browser (roving tabindex and `aria-expanded` toggling both confirmed via actual keyboard/click events, not just read as code). **Explicitly not done**, and worth doing before any client-facing move: ~29 sites across the questionnaire where a visible label and its input aren't formally associated via `htmlFor`/`id` (a lesser gap than the ones fixed — a screen reader can still read the nearby text in browse mode); full tokenisation of the remaining ~170 hardcoded hex literals (a design-system tidiness item, not a contrast failure); a GDPR/DPIA review of `app/privacy/page.jsx` against what actually leaves the system.

**Two bugs found by reviewing a real generated report.** After all of the above, a real report was read end-to-end (all 16 pages, every number re-derived by hand) and turned up two more issues, both fixed and verified against a freshly-regenerated report and its actual rendered PDF: (1) the AI had fabricated a specific, false claim that "the automated sense check raised a warning" about an NRM1 percentage rule — no such warning code exists (cross-checked against all seven real codes in `senseCheck.js`) — fixed with the thirteenth `AI_SYSTEM_PROMPT` rule described above; (2) two of the PDF's sixteen pages were wasted, near-blank pages caused by a missing `break-inside: avoid` on the section-header print CSS, described under "Report output" above.

**September 2026 assessment pass (second round).** A full reassessment of the app, questions and scope picker was written up and then implemented as five phases at £0 (see the plan's Context section for the list). Everything below is done, built, linted, unit-tested (164 Vitest cases as of the honest-progress-and-optionality slice, 2 skipping until the workbook is updated) and baselined (`scripts/__baseline__/before-phase1` → `after-phase4`: only the intended float/inflation deltas moved; phases 2–4 IDENTICAL):

- *Trust:* percentage rule trace + "How each percentage was derived" table; workbook base date and rate `Source` column (printed, never the generation date); confidence-linked range width (Tab 9); programme float applied with best case shown; FastTrack levers FT2–10 offered; Procurement decision sheet replaces hard-coded route/contract bands. Prompt now carries the build-up, the acceleration options, and "use exactly" for tender type / design responsibility.
- *Bugs:* the tier-2 preset ticking unpriceable `5.1b` (fixed at source by `priceable` flags in the picker, and in the workbook spec); External Works asks for site area; workbook plumbing no longer leaks into quantity labels; `per_kw` pricing type.
- *UK checks:* Q3.8 (conservation area, party wall, ecology) with fixed-rating risk seeds `CTX-*`, Tab 3 `higher-risk` rules, `BS1` Gateway 2 stage, `SV7` ecology survey; Q4.3a VAT position in the budget verdict; storeys to 7+. (A later September 2026 question-gating pass moved higher-risk-building status itself out of Q3.8 and onto a Section 1 derivation — Q1.2a storeys, Q1.3 building use, and a new Q1.6 height question — gated so it no longer applies to Demolition only or External works only; see `isHigherRiskBuilding()` in `lib/siteContext.js` and `lib/questionSets.js`.)
- *Questionnaire/report:* Q6.1 is an exclude-list and cost is never optional; Q4.4 capped at two with a primary; Q4.0 start date with calendar dates everywhere; `/sample` public report; number-leak guard on prose.
- *Features:* `/api/compare` + on-screen scenario panel; `/api/suggest-scope` + "Suggest scope from my objective".
- **The workbook side is the user's to apply** from `docs/workbook-changes-sept-2026.md`; until then the new parsers fall back to the previous behaviour and `/api/rates-check` reports which additions are still missing (`baseDate`, `rangeWidthsSheet`, `sourceColumnPresent`, `procurementSheet`, `newDurationRows`).

**NRM1 v5 scope catalogue (23 September 2026, PR #12 — see "Current state" at the top for status, deployment and open items).** Built from "Scope of works — brief for Claude Code" (23 September 2026) and its eight confirmed decisions: new workbook loader with checks and last-good fallback, the quantity-rule evaluator, the shared scope engine, the cost engine rebuilt on v5.2, the four-level picker (`ScopePicker.jsx`), v4.5-code migration for drafts and KV reports, "When selected" risks as deterministic seeds and assumptions in the prompt and cost assumptions, per-line Estimated / your-figure marking and † AI-estimate flags in both renderers, BCIS-group totals, BCIS 12 below the line. The seven brief scenarios are Vitest cases. An Extension stale-level bug found in review was fixed with a regression test, and workbook v5.3 then filled the v5.2 data gaps (below).

**What's outstanding**, roughly in order of what's most worth doing next:
- **Workbook v5.3 (23 September 2026)** filled the v5.2 gaps by editing only the named cells in the sheet XML (no SheetJS round-trip; styles, validation and formulas untouched, filename kept): Solar PV / Battery storage Ext Works rates (= NB Std, derivation in Source detail), External works only pre-ticks (the app's previous EW preset), ▶ range_widths (illustrative), the M&E rule and golden rules relabelled Q2.2/Q2.3, version 5.3 and an update-log line. **Other or mixed deliberately has no typical scope** — the app never offered one ("no honest default" for the catch-all), and `/api/rates-check` still lists it under `noTypicalScope` for that reason. Still open: 47 rates and every quantity ratio are "AI estimate – verify" (decision 8 — flagged on the report until checked); the range-width factors are illustrative.
- Apply the Programme half of `docs/workbook-changes-sept-2026.md` (v4.4: Procurement sheet, SV7/BS1 rows) and re-run `npm test`. Its NRM1 v4.6 half is superseded by v5.x (the range widths now live on '3. Settings' ▶ range_widths). Not yet carried into v5.x from that spec: the three illustrative higher-risk-building percentage rows and the extra benchmark bands (Extension, Fit-out education/healthcare, External works).
- Consider Vercel Pro (Hobby's terms prohibit commercial use, and Pro removes the 60s ceiling the two-phase split works around) and a price book to source the rates — the two paid items from the assessment.
- The ~29-site label/`htmlFor` association gap and the full hex-literal tokenisation noted under Phase 5.
- `checkJs`/TypeScript conversion for `lib/*.js` — the plan calls this "incremental," not urgent; `allowJs` with no `checkJs` means most of the codebase's real logic is still untyped.
- A GDPR/DPIA review of `app/privacy/page.jsx`.
- A small number of pre-existing cosmetic lint items (a few `<a>`→`<Link>` swaps, some unescaped quotes) — deliberately left alone rather than rushed through pages that had just been carefully verified.
- The Word-doc PDF/`.docx` structural parity between `reportBuilder.js` and `ReportRenderer.jsx` should be spot-checked periodically — they're two independent implementations of the same report and have drifted before (the Q6.1 section-selection key mismatch, fixed earlier, is the precedent).
